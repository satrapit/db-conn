<!DOCTYPE html>

<html lang="{{ get_bloginfo('language')|default('en') }}" dir="{{ is_rtl() ? 'rtl' : 'ltr' }}">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>{{ page_title }}
			-
			{{ site_name }}</title>
	</head>
	<body>

		<p>{{ __('This is a demo panel page.', 'db-conn') }}</p>
	</body>
</html>
