{% extends "layouts/login-layout.twig" %}

{% block content %}
	<div class="space-y-4">
		<div>
			<label for="username" class="block text-sm font-medium text-gray-700 mb-2">
				{{ __('Username', 'db-conn') }}
			</label>
			<input type="text" id="username" name="username" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500 outline-none transition-colors" placeholder="{{ __('Enter your username', 'db-conn') }}">
		</div>
	</div>
{% endblock %}
